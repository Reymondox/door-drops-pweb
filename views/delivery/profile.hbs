<section class="py-4">
  <h2 class="mb-3">Mi perfil</h2>

  <form method="post" action="/delivery/profile" class="card shadow-sm p-3" enctype="multipart/form-data">
    <div class="row g-3">

      <div class="col-12 col-md-4 d-flex flex-column align-items-start">
        <label class="form-label">Foto de perfil</label>

        <div class="d-flex align-items-center gap-3">
          <img
            src="{{#if delivery.imageUrl}}{{delivery.imageUrl}}{{else}}/assets/images/user-photos/default-avatar.png{{/if}}"
            alt="Foto de perfil"
            class="rounded"
            width="120"
            height="120"
            style="object-fit: cover;"
          />

          <div>
            <input type="hidden" name="imageType" value="userImg"/>
            <input type="hidden" name="deliveryId" value="{{delivery.id}}"/>

            <input
              type="file"
              class="form-control"
              name="imageFile"
              accept="image/*"
            />
            <small class="text-muted d-block mt-1">Formatos: JPG/PNG. Opcional.</small>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <label class="form-label">Nombre</label>
        <input class="form-control" name="deliveryName" value="{{delivery.name}}">
      </div>

      <div class="col-md-4">
        <label class="form-label">Apellido</label>
        <input class="form-control" name="deliveryLastName" value="{{delivery.lastName}}">
      </div>

      <div class="col-md-4">
        <label class="form-label">Teléfono</label>
        <input class="form-control" name="deliveryPhoneNumber" value="{{delivery.phoneNumber}}" required>
      </div>

      <div class="col-md-8">
        <label class="form-label">Correo (solo lectura)</label>
        <input class="form-control" value="{{delivery.email}}" disabled>
      </div>
    </div>

    <div class="d-flex gap-2 mt-3">
      <button class="btn btn-success" type="submit">
        <i class="bi bi-check2-circle"></i> Guardar
      </button>
      <a class="btn btn-outline-secondary" href="/delivery/home">Cancelar</a>
    </div>
  </form>
</section>